# Desktop (PySide6) build — install alongside requirements.txt.
# The Streamlit web app does NOT need these; keep them separate so the
# Docker image stays lean.
-r requirements.txt
PySide6
PySide6-WebEngine ; platform_system != "Linux"   # map view; on Linux it ships in PySide6
