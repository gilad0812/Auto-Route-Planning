# Desktop (PySide6) build — install alongside requirements.txt.
# The Streamlit web app does NOT need these; keep them separate so the
# Docker image stays lean.
-r requirements.txt
PySide6
pyvista          # 3D terrain view (step 2)
pyvistaqt        # embeds the PyVista render window in Qt (step 2)
PySide6-WebEngine ; platform_system != "Linux"   # map view (step 3); on Linux it ships in PySide6
