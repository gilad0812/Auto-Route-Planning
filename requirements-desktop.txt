# Desktop (PySide6) build. Install: pip install -r requirements-desktop.txt
-r requirements.txt
PySide6
# Map is a native QGraphicsView canvas (offline) — no QtWebEngine needed.
pyinstaller   # packaging into a standalone .exe
