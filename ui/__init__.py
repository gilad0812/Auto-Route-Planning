"""PySide6 desktop UI for the LiDAR drone route planner.

Step-1 skeleton: the parameter sidebar + compute loop + results/safety panel,
wired to the existing framework-free model in ``src/`` (route_planner,
density_estimate, safety). The map and 3D views are stubbed and land next.
"""
